<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient android:startColor="@color/stage_add"
                android:centerColor="@color/stage_tint_text"
                android:endColor="@color/stage_add"
                android:angle="90"/>
            <stroke android:width="1dp" android:color="@color/blue_tint_state"/>
            <corners android:radius="20dp"/>
        </shape>
    </item>
</selector>